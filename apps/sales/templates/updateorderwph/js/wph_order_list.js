
function downcsv(o){
    var url = $(o).attr('data-url');
   // debugger;
    location.href = url;
}
function deldowncsv(o){
    var url = $(o).attr('data-url');
   // debugger;
    location.href = url;
}
function wphdow(o){
    var url = $(o).attr('data-url');
   // debugger;
    location.href = url;
}
